# Data: large/generated, not versioned. Regenerated by the pipeline or
# transferred separately; kept on disk under data/ but not tracked in git.
data/

# Archives dropped in for transfer
*.zip

# Python
__pycache__/
*.pyc

# Editor/OS
.DS_Store
